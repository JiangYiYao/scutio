# 运行与不确定性处理：修复验证

依据：2026-09-06 的“研究祖龙娱乐2026至2027年”对话。此次验证针对执行路径与推理方法，不重新确认该对话全部市场事实。

## 修复范围

- Windows 新对话可从已安装技能找到明确配置、用户 venv 或链接所指开发仓库的解释器。离线探针区分缺失解释器、启动失败、导入失败和超时，不取行情、不安装包、不写配置。
- 启动失败先辨别权限，允许按宿主机制申请同一检查的执行权限；不通过复制解释器或改变数据根规避隔离。
- 限定域名搜索、原始列表覆盖、名称与主体匹配写入查证说明。检索缺口与对象新发生的变化分开。
- 排期研究区分计划、实际节点、收入和利润。观察日期需要依据；当前可调查证据与事后验证分开，条件推演接到收入和费用。
- 安装后的验证从只导入 requests/pandas 改为检查声明依赖与数据包。安装权限与实际 Agent 权限分别验证。

## 独立行为试跑

由独立 Agent 只读取技能及相关说明，在不联网、不读取评测预期和原对话的情况下回答两组虚构材料。原始回答保存在本次工作区 `.codex-tmp/milestone-forward-check.md`；以下记录输入要点与可核对结果。

| 案例 | 原始材料要点 | 实际回答观察 |
|---|---|---|
| 上线进度与检索覆盖 | 当前9月6日，公司8月19日仍计划11月上线、9月20日公开测试；所给官方页只到8月31日，论坛延期传闻无原公告 | 保留公司计划的支持作用，指出9月20日尚未到；没有将9月索引缺失说成没有公告或实际延期，也没有自行指定额外截止点 |
| 跨期经营影响 | 全年老业务贡献毛利1200万元，固定费用1500万元；新品每月收入200万元、贡献毛利率60%；100万元宣传费固定当年12月确认 | 10月1日上线经营亏损40万元，次年1月上线当年亏损400万元；两种情况均扣当年宣传费，差额360万元；没有转成PE、现金流或事件发生概率 |

两个案例均给出了可检查的局部结论。它们是修复后的定向试跑，没有相同输入下的旧版本盲测，不能据此声称整体研究质量或偏向已经得到统计验证。

## 回归范围

行为案例从36条扩展为44条，补充了已确认延期的反向案例、比较口径、无依据截止点和启动权限场景。JSON校验只验证案例结构，不意味着44条均已执行。

程序回归覆盖离线探针、空间路径下的复制安装、从无关工作目录执行、显式错误路径不被静默替换、启动失败与导入失败区分，并继续运行原有数据、记录和安装测试。

最终执行：322项通过、22项跳过；跳过项为未启用的联网测试及平台权限相关测试。存在预期的模拟源失败警告和交易日历依赖的弃用警告，没有测试失败。技能结构校验及两个安装脚本语法检查通过。

实际环境中补齐了缺少的 httpx、xlrd、exchange-calendars、pypdf，并将超出 requirements 范围的 pandas、lxml 调整至约束内。已安装 Junction 入口在允许执行的权限下自动定位仓库 venv，返回 ready；同一探针在默认受限权限下能报告 startup_failed 及 base Python 路径。权限限制仍由宿主管理，不声称通过代码修改消除了隔离。

单次公网冒烟通过 `security_quote(['hk09990'])` 实际执行，返回 `ok=true`、`source=tencent`。该结果验证这次行情执行路径恢复，不代表所有数据源或财务事实已验证。
