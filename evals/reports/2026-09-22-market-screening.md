# A 股证券池与特征筛选：真实取数验收

日期：2026-09-22；2026-09-23 补充带 Key 路径、最终回归与独立调用验证。本报告记录本轮开发工作区中的实际取数和契约检查；执行开始时 HEAD 为 `0e17d6f`，本轮实现当时尚未提交，该提交号不是本轮全部代码的版本标识。此前冻结报告保持不变。

## 验收范围和当前结论

沪深北官方名单已经真实获取，并通过 `stock_universe()` 归一为 5,566 个唯一 A 股证券身份。名单没有以成功报价作为入选条件。已有名单结果也实际接入了仅按交易所筛选的 `screen_market()`，得到 345 个北交所证券。

联合真实请求已完成：5,566 只证券分成 56 批取报价，取得 2026 年及 2025 年半年报横截面并计算同比，全程 37.680 秒。两只证券因收入缺失或没有兼容报表保持未知；展示字段另有独立缺口。以下记录实际验证的字段，不宣称目录中的 33 个特征都已逐只核验，也不把源完整性未知解释成全市场无遗漏。

| 检查 | 实际执行状态 | 证据边界 |
|---|---|---|
| 沪主板、科创板、深 A 股、北交所名单真实请求 | 已完成 | 四份官方名单经 AKShare 获取 |
| 名单唯一性、字段身份、当前范围、缓存时点 | 已完成 | 5,566 个唯一规范 symbol，必需身份字段均非空 |
| 仅名单条件的筛选整合 | 已完成 | 北交所条件命中 345，未请求报价或财务 |
| 全市场报价与财务联合条件整合 | 已完成 | 5,566 个输入、5,564 个条件命中、2 个未知，额外字段分别报告缺失 |
| 带 Key 的报价和免费补字段 | 已完成 | 五只证券同批返回，复用官方名单身份；缺少行情时点时保留实际补取来源 |
| 财务字段与源表一致性 | 已完成抽样 | 五只证券的收入、归母净利润、ROE、毛利率共 20 个值与原始响应相符；不是与公司公告逐项独立核验 |
| 全市场全部字段非缺失 | 未作此断言 | 未取得和不可用字段必须保留缺口 |
| 历史时点证券池、当时已披露信息重建 | 不支持本次验收 | 当前名单和当前可取得财务材料不是历史回测数据集 |
| 筛选规则的投资效果或机会发现质量 | 未评测 | 技术取数和条件执行不证明收益有效性 |

## 名单来源与真实结果

运行使用已安装 AKShare `1.18.94`，先检查该版本函数源码，再核对 [AKShare 官方源码](https://raw.githubusercontent.com/akfamily/akshare/main/akshare/stock/stock_info.py)。请求经过现有隔离 worker、执行额度和一小时共享快照缓存；使用临时 `SCUTIO_HOME` 与临时配置目录，没有读取用户凭据。

首次受限沙箱调用四个公开接口均遇到连接错误，没有将失败当成合法空市场。获准出网后，相同公开接口均返回有效名单。后续归一和筛选 smoke 使用这些真实响应的缓存，没有为获得有利结果更换候选来源。

| 范围 | 函数及参数 | 原始行数 | 输出 A 股数 | 首次快照时刻（UTC） |
|---|---|---:|---:|---|
| 沪市主板 A 股 | `stock_info_sh_name_code(symbol="主板A股")` | 1,702 | 1,702 | 2026-09-22 13:08:30.379211 |
| 沪市科创板 | `stock_info_sh_name_code(symbol="科创板")` | 618 | 617 | 2026-09-22 13:08:30.215797 |
| 深市 A 股 | `stock_info_sz_name_code(symbol="A股列表")` | 2,902 | 2,902 | 2026-09-22 13:08:31.167800 |
| 北交所 | `stock_info_bj_name_code()` | 345 | 345 | 2026-09-22 13:08:35.659931 |
| 合计 | 四份名单合并 | 5,567 | 5,566 | 各行保留其实际快照时刻 |

官方范围入口分别为 [上交所股票与存托凭证列表](https://www.sse.com.cn/assortment/stock/list/share/)、[深交所股票列表](https://www.szse.cn/market/product/stock/list/index.html) 和 [北交所上市公司列表](https://www.bse.cn/nq/listedcompany.html)。这些网页是上游范围依据；本轮取得的机器数据来自对应 AKShare 官方适配函数，而非网页搜索摘要。

最终 `stock_universe()` 结果为 `ok=True`、`errors={}`、`partial=False`、`is_complete=None`、`completeness="unknown"`。沪市合计 2,319、深市 2,902、北交所 345；唯一 symbol 数与输出行数同为 5,566。每行的 `symbol`、`code`、`name`、`exchange`、`asset_type`、`listing_status`、`as_of` 和 `source` 均非空。

名册中含 `ST` 字样的名称有 204 个，保留在证券池内。真实取数没有将停牌列表逐项交叉核验，因此本报告不声称已独立证实停牌证券总数；实现不根据交易状态或报价成功与否删除证券，离线测试覆盖了含停牌标记的输入行保留行为。

### 发现并修正的身份边界

首次归一发现深交所正式名单中的 `302132` 中航成飞被已有身份校验拒绝：旧规则只允许深市 `000/001/002/003/300/301` 公司代码前缀。通过名单原始行和 [深交所披露的中航成飞公告](https://disc.static.szse.cn/download/disc/disk03/finalpage/2026-06-09/9e9ccc82-e096-48d0-872c-e660a807f4ac.PDF) 核验后，将 `302` 加入身份规则，并增加名册回归测试。修正前输出 5,565 行，缺口显式记录；修正后输出 5,566 行且 `sz302132` 在池中，没有静默丢弃异常行。

科创板官方列表含 `689009` 九号公司。该证券为境内存托凭证，见 [上交所存托凭证上市公告](https://www.sse.com.cn/disclosure/announcement/listing/c/c_20201027_5242851.shtml)。本入口限定严格 A 股，因此排除该行，并记录 `scope_exclusions={"cdr": 1}`；没有将其标为普通 A 股或把它算作获取失败。当前范围也不包含 B 股、基金、指数和已退市证券。

### 完整性与时点含义

| 交易所 | 已核对的适配器获取方式 | 返回的 `source_complete` | 尚未取得的证据 |
|---|---|---|---|
| 沪市 | 主板与科创板分别请求第 1 页，页大小 10,000 | `None` | AKShare 返回行表时丢弃上游总数和分页响应；本次未独立校验总数 |
| 深市 | 官方 `SHOWTYPE=xlsx` 的整份 A 股导出，无客户端分页或截取 | `True` | 表示源导出已完整读取；未与另一个独立名册交叉核验 |
| 北交所 | AKShare 根据上游 `totalPages` 遍历各页 | `None` | 合并后不返回每页行数、实际页号或总数证据，无法独立排除分页重复与漂移 |

所有交易所的 `independently_verified` 均为 `False`。已知请求失败、空名单、无效身份或重复冲突会成为明确错误，并将有关范围标为 `partial`；没有这些已知错误不等于已证明全集完整。因此本轮整体完整性保持未知，没有凭 5,566 个成功身份或可获取报价断言“全市场无遗漏”。

`as_of` 表示名册响应被取得的时间，缓存命中继续使用原时刻，`as_of_basis="directory_retrieved_at"`、`historical_membership=False`。该时刻不是交易所声明的历史名单生效日。四份快照分别保留时间，顶层 `as_of` 为其中最新者。

## 财务特征通路

本段取数时序、单行原始字段验证和测试结果依据财务通路执行者的实际运行回报；报告整理者另外只读检查了该次保存的原始行表，复核了行数、唯一代码数和下表样本值，没有重新发起整份财务请求。

2026-09-22 21:09:01（Asia/Shanghai），AKShare `1.18.94` 的 `stock_yjbb_em(date="20260630", _snapshot=True)` 实际成功返回 11,450 行，股票代码唯一数也为 11,450。报告期为 2026-06-30，即上半年累计口径，不能当作单季或 TTM。此前受限沙箱请求出现代理/连接错误，公开网络 direct 路由重试成功，错误没有被抹成空数据。

官方适配器使用东方财富 `RPT_LICO_FN_CPD`，按 `REPORTDATE` 过滤，页大小为 500，先取得总页数再遍历。按本次行数和源码可以推算约 23 个数据页加一次初探，但未对内部 HTTP 逐次计数，因此这不是实测请求次数。适配器不保留原始总数、各页证据、交易所、证券类型和上市状态，不能据此声明全量完整或把该财务表充当上市证券池。数据中确实包含大量场外证券，筛选入口必须先取得官方在市名单，再按规范证券身份相交。

| 原始样本 | 收入（人民币元） | 归母净利润（人民币元） | ROE（%） | 毛利率（%） | 基本 EPS（元/股） |
|---|---:|---:|---:|---:|---:|
| 600519 贵州茅台 | 92,278,072,083.21 | 44,516,880,421.86 | 16.75 | 89.5552128279 | 35.57 |
| 000001 平安银行 | 70,617,000,000.00 | 25,696,000,000.00 | 5.22 | `null` | 1.24 |
| 302132 中航成飞 | 15,167,213,559.04 | 709,070,704.88 | 4.93 | 11.0420778902 | 0.2654 |

执行者另外对 `600519` 请求同一个公开报表接口，核对归母净利润为 `PARENT_NETPROFIT`、ROE 为 `WEIGHTAVG_ROE`、EPS 为 `BASIC_EPS`。这为这些字段的含义提供直接源端证据；未据此宣称全部行业和全部报表字段均已逐条与公司公告复核。平安银行缺少毛利率是实际源缺失，不能以 0 代替。

该单行核验请求为 [东方财富公开数据接口](https://datacenter-web.eastmoney.com/api/data/v1/get)，参数 `reportName=RPT_LICO_FN_CPD`、`columns=ALL`、`pageSize=1`、`pageNumber=1`，过滤 `REPORTDATE='2026-06-30'` 与 `SECURITY_CODE="600519"`，不使用 token。响应还给出 `SECUCODE="600519.SH"`、`SECURITY_TYPE="A股"`、`REPORTDATE="2026-06-30 00:00:00"`。

接口中文列名“最新公告日期”实际映射 `UPDATE_DATE`，不是首次披露时间 `NOTICE_DATE`。当前材料查询保留 `time_basis="retrospective_current_materials"`，不能解释成某历史筛选时点当时已知的财务值；若使用更正后资料，可能存在前视信息。贵州茅台与平安银行这份行表的更新日为 2026-08-15，中航成飞为 2026-08-24。

茅台单行原始响应的 `UPDATE_DATE` 与 `NOTICE_DATE` 恰好同为 `2026-08-15 00:00:00`。两列语义的区别来自已核查的适配器映射与原生字段，并非声称这个样本实际显示了两个不同日期。

首次公共门面归一运行回报 11,206 个候选、244 行拒绝。当 `302` 身份规则修正后，报告整理者对同一份保存的真实响应直接执行当前 `_normalize_rows()`，得到 11,207 个候选、243 行拒绝、重复数 0，并确认含 `sz302132`。这是保存响应的本地再归一验证，不是第二次全量网络请求。候选仍含场外证券，该计数或它的缺失率都不能解释成当前 A 股全集覆盖率。

在这份 11,207 行的未作上市名单交集的候选表中，各字段缺失计数为：收入 49、归母净利润 0、源收入同比 100、源净利润同比 0、ROE 277、毛利率 259、EPS 2、每股经营现金流 5、每股净资产 1。这是该源行表的质量诊断，不能用来宣称某个筛选条件覆盖了全部在市 A 股。另有 1 行标记 `future_provider_update_date`；整合筛选需要按字段状态排除其可用性，而不能仅因有数值便视作已披露。

## 已执行的筛选整合 smoke

使用上述真实名单缓存执行：

```python
screen_market({"exchange": "bj"}, fields=["name"], limit=3)
```

实际返回：输入 5,566，条件命中 345，展示 3，`missing_count=0`，`run_state="finished"`。展示样本为 `bj920000` 安徽凤凰、`bj920001` 纬达光电、`bj920002` 万达轴承。这里只核对身份和名单条件，展示顺序不构成投资排名。

筛选结果的 `partial=True`，`coverage.universe_complete=None`、`coverage.screen_complete=None`。名单接口的 `partial=False` 表示无已知名册错误，筛选接口的 `partial=True` 另外包含全集完整性未知；本次结果没有将这种未知升级为完整市场结论。

## 全市场报价与财务联合执行

2026-09-22 21:26:26（Asia/Shanghai）开始，在隔离的临时配置和缓存中使用免费公开来源，未读取用户凭据。实际请求为：

```python
screen_market(
    {"mcap_yi": {"gt": 0}, "revenue": {"gt": 0}},
    report_date="2026-06-30",
    fields=["pe_ttm", "pb", "roe", "net_profit", "gross_margin",
            "industry", "revenue_growth_pct", "net_margin_pct"],
    sort_by="mcap_yi", limit=None,
)
```

条件有意宽松，用于检验取数与缺口传播，不代表选股策略。返回 `ok=True`、`run_state="finished"`、`input_count=5566`、`matched_count=5564`、`excluded_count=0`、`missing_count=2`。名单完整性仍未知；存在已知字段缺失，因此 `partial=True`、`coverage.screen_complete=False`。

| 阶段 | 实际结果 | 耗时 |
|---|---|---:|
| 报价 | 56 个任务全部成功，每批最多 100 只；首批成功 0.516 秒 | 7.118 秒 |
| 财务 | 本期 2026-06-30、上年同期 2025-06-30 两个任务成功 | 18.931 秒 |
| 总过程 | 包含名册获取、归一和筛选 | 37.680 秒 |

这里的 56 批和两期是门面任务数，不是底层 HTTP 请求数；财务适配器内部存在分页。耗时只代表本次网络和缓存条件，不构成速度保证。

| 请求字段 | 可用 | 缺失 |
|---|---:|---:|
| 总市值、PE、PB（分别） | 5,566 | 0 |
| 收入 | 5,564 | 2 |
| 归母净利润、行业（分别） | 5,565 | 1 |
| ROE | 5,510 | 56 |
| 毛利率 | 5,462 | 104 |
| 归母净利润/营业总收入 | 5,564 | 2 |
| 同期收入增长率（正基数） | 5,563 | 3 |

影响条件判断的两只证券是 `sh688759`（来源收入为空）和 `sz002731`（当前财务源未返回兼容报表）；已检查保存的原始表，未将两者改为收入 0 或不符合条件。ROE、毛利率等仅用于展示的缺口不会把条件已命中的证券改成不符合，但会让请求字段完整性保持为假。

以下五只覆盖沪主板、科创板、深主板、302 新前缀和北交所。收入、归母净利润、ROE、毛利率四个字段逐一与本次保存的上游行表核对相符；表内金额为便于阅读换成亿元，程序原值为人民币元。同行情与同一来源财务表的一致性核对不等于跨来源验证，也不是公司公告审计。

| 样本 | 市值（亿元） | 收入（亿元） | 归母净利润（亿元） | ROE（%） | 毛利率（%） | 同期收入增长（%） |
|---|---:|---:|---:|---:|---:|---:|
| sh600519 贵州茅台 | 15,673.52 | 922.7807 | 445.1688 | 16.75 | 89.5552 | 1.3001 |
| sh688981 中芯国际 | 10,481.07 | 386.3512 | 44.6743 | 2.90 | 23.1558 | 19.4357 |
| sz000001 平安银行 | 2,272.43 | 706.1700 | 256.9600 | 5.22 | 缺失 | 1.7756 |
| sz302132 中航成飞 | 1,601.92 | 151.6721 | 7.0907 | 4.93 | 11.0421 | -26.7360 |
| bj920000 安徽凤凰 | 13.18 | 2.0912 | 0.2793 | 4.14 | 26.6034 | 3.8877 |

行情字段保留腾讯源时点，财务字段保留本期及上期报告期、来源、更新日期口径和抓取时间。平安银行毛利率实际为空，输出继续为 `None`。

## 带 Key 的五只证券验证

2026-09-23 使用用户已配置的凭据验证上述五只证券，凭据仅在内存传递，没有写入报告或临时配置。直接调用 `security_quote(..., sources=("hithink",), identity_records=...)` 复用已经取得的官方名单身份，0.432 秒返回五只证券，`source="hithink"`，没有缺失或报错。对客户端逻辑请求路径的记录只出现一次 `/api/a-share/prices/snapshot`，没有逐只 ticker 搜索；该记录不等同于底层 HTTP 计数。

随后从新取得的全市场名单执行：

```python
screen_market(
    {"symbol": {"in": sample_symbols}, "price": {"gt": 0}},
    fields=["name", "pe_ttm", "pb"], limit=None,
)
```

其中 `sample_symbols` 为上表五个完整 symbol。新一天的名单为 5,567 只，不能与前一天的 5,566 只混作同一快照。结果为命中 5、排除 5,562、未知 0；先使用名单条件排除其他证券，只为剩余五只取报价。包含全新名册获取共耗时 12.139 秒，所有请求字段可用，但全集完整性仍未知。

Financial API 实际快照未提供可核验的行情时间及所需估值字段，筛选器据此补取免费报价。本次最终 `price/pe_ttm/pb` 的 `field_sources` 都是腾讯，不能因配置了 Key 就把它们描述成同花顺行情。两次操作累计只观察到两个快照逻辑调用，没有逐证券身份查询。此实验验证了带 Key 分支、身份复用和字段补取，不证明同花顺所有字段、配额或稳定性。

## 离线测试与独立调用验证

实际执行名单、AKShare 网络契约与证券身份测试，共 357 项通过。等价命令如下，运行时关闭测试报告写出和 pytest 缓存：

```sh
SCUTIO_TEST_REPORT=0 "$SCUTIO_PYTHON" -m pytest -p no:cacheprovider \
  tests/data/test_universe.py \
  tests/data/test_akshare_network.py \
  tests/data/test_security_identity.py -q
```

其中名单测试覆盖：分市场/分板块失败不抹掉成功范围、空目录不冒充空市场、停牌和 ST 保留、CDR 范围排除、缓存时刻保留、错误资产/交易所拒绝、冲突重复拒绝、页面容量风险和 `302132` 回归。`git diff --check` 通过；执行环境未安装 Ruff 模块，本轮名单执行者未据此声称 lint 通过。上述结果不是完整仓库测试结果。

财务通路执行者回报的已执行测试为：`test_financial_snapshot.py` 与 `test_akshare_market_contracts.py` 合计 55 项通过；`test_hk_us_financials.py` 与 `test_akshare_network.py` 合计 73 项通过，其中新增财务快照用例 19 项。后两组与上方 357 项中存在文件重叠，不能直接相加当作唯一通过用例总数。随后新增筛选门面的 31 项离线用例也实际通过；这些夹具测试依然不代替真实行情和财务整合取数。

整合复核发现并已修正：指数范围丢失已有名称和来源、名单字段抓取时间被顶层时刻覆盖、取消信号未覆盖获取名单阶段，以及财务日期冲突、重复身份、推断身份被用于带 Key 报价复用等问题。对应夹具覆盖源失败、身份冲突、日期不可用、同比基数、预先取消和进度状态。

过滤条件与请求展示/排序字段的完整性现在分开计算。新增回归覆盖条件已命中而额外字段缺失的情况；文本字段不能冒充数值排序，非有限阈值及非法报告期在取数前拒绝。纯记录筛选、在线筛选和命令行包装使用同一条件实现，没有保留旧 `local_stock_screen` 实现。

独立执行者仅阅读实际 skill、调用文档和离线字段目录，收到请求：“2026 年半年报收入比上年同期增长至少 20%，市值 100 亿到 1,000 亿；按增长率降序展示 20 只，再给 PB。先写调用，不联网；能否加 AI 业务订单增长？”实际给出 `revenue_growth_pct.min=20`、`mcap_yi.min=100/max=1000`、`report_date="2026-06-30"`、`fields=["name", "pb"]`、按收入增长降序、`limit=20` 的正确调用，并解释为 2026 年与 2025 年上半年、正基数、当前行情。它明确 AI 业务订单增长不在自动目录内，需要核实材料、结构化后使用 `screen_records`，没有用 AI 关键词替代订单增长。未执行网络请求或修改文件。

这次定向调用验证检查文档可用性与能力边界，不能证明模型在开放研究中会稳定选出有效策略，也不能替代新旧版本研究质量对照。

最终整合代码在 Python 3.12 下执行 `pytest -q -p no:cacheprovider -m 'not live' tests --tb=short`，结果为 **1,400 项通过、6 项跳过、13 项未选中**，耗时 34.30 秒；这是唯一用例总数，不与前述交叉分组累加。随后文档修正后的调用契约与示例测试 22 项通过。Ruff 检查与格式检查、skill 结构检查、公开产物检查通过。公开产物检查只覆盖当前文件，没有扫描 Git 历史。
