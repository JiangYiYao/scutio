# Financial API contract fixtures

Captured from authorized read-only requests on 2026-09-09. Only response bodies are retained; no request headers or credentials. Fields deliberately preserve provider-native financial scope, including ICBC cash distributions and CATL special dividends. Used for parser and fallback regression tests; values are historical samples, not current recommendations.
